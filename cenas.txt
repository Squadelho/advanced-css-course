quando existe um seletor float é necessario usar "clearfix"
selector::after{
  content:"";
  clear: both;
  display: table;
  }


NPM

npm init  --> criar package.json
npm install --save-dev --> instalar sass como devDependencie
npm install --save --> instalar sass como Dependencie
npm install --> instala tudo o que estiver na package.json

SASS usando live sass compiler 

sass -w dir.scss dir.css